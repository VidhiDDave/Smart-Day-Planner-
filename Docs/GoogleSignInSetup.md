# Google Sign-In Setup

This project will use Google Sign-In for user authentication.

## Current Status

Google Sign-In package setup is being added first.

The app still uses mock sign-in until the real OAuth configuration is added.

## Package

Package URL:

```text
https://github.com/google/GoogleSignIn-iOS


## Configuration Status

The app includes a Settings section that shows whether Supabase and Google Sign-In configuration values have been added.

This is intended to make it easier to verify setup before replacing the mock auth flow with real Google/Supabase authentication.
